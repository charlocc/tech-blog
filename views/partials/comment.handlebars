<div class="text-center" id={{user.id}}>
    <div class="card review-card">
        <div class="card-header">
            <button class="btn btn-sm box" data-id="{{id}}">X</button>
        </div>
        <div class="card-body">
                <p>{{user.username}} : "{{note}}"</p>
        </div>
    </div>
    </br>
</div>
<script src="/js/comment.js"></script>